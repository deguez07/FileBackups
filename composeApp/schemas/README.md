# Room schema exports

This folder is the `schemaDirectory` for `composeApp` (Room Gradle plugin). JSON files here describe the database shape per version; commit them when you bump `@Database` version or run migrations.
